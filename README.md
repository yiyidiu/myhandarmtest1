# v0.4.0-self-collision-safe

重建自 2026-08-25 21:03（Asia/Shanghai）的全机器人自碰撞安全检查点。

## 冻结时证据

- 相关 Catkin 构建、Python/XML 检查通过；
- 145 项回归测试通过；
- 动态机械臂预接触停止和自动回退通过；
- 手部碰撞目标在 49/55 采样处被拒绝；
- 空闲 5 秒机械臂和手部关节跨度为 0 rad；
- Servo → 严格守卫 → 控制器的发布拓扑通过。

## 原始冻结包

`self_collision_safety_passed_20260825_210302.tar.gz`

SHA-256：`43e5e953d109add1e2fbea397d3e933e30a7260191b23314a3c39a67c5bc20fd`

检查点范围是机器人自碰撞；外部场景碰撞、D455 最终真人链和实体硬件验收不在此结论内。

## 重建复验与已知缺陷

- 原冻结包漏装了测试明确依赖的手掌减面碰撞网格、Xacro 和生成 URDF；重建版从工作空间中保留的 2026-08-25 对应文件补齐；
- 独立 Catkin 构建通过；Catkin 登记的 123 项测试全部通过；
- 感知目录 201 项离线测试全部通过；
- 扩展执行当时未纳入一键回归的完整仿真目录时，34 项中出现 9 个错误：旧 `grasp_geometry.py` 不支持新的手掌 `Mesh` 碰撞几何；
- 该历史缺陷在 `v1.0.0-clean-baseline` 中通过显式失败关闭 box proxy 和回归测试修复，不反向改写本检查点的算法源码。
