# 先读这里

请先阅读 `GPT_PROJECT_HANDOFF_20260815.md`。它是 2026-08-15 当前状态的权威摘要。

本包里某些旧报告保留历史过程，可能含已被后续配置替换的参数。审阅优先级为：

1. `GPT_PROJECT_HANDOFF_20260815.md`；
2. 当前 `src/` / `perception_hamer/` 源码与配置；
3. `selected_results/` 中最新真实 JSON；
4. 其他历史报告。

最重要的边界：完整物理抓取—抬升—放回—松开—撤离目前是 **NOT PASS**。不要用历史 fixed-joint attachment 演示、局部 grasp-only 结果或单元测试把它改判为 PASS。
