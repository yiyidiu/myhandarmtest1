# 2026-08-16 至 2026-08-25 项目节点与里程碑

## 证据口径

工作空间在 2026-08-25 清理前不是 Git 仓库，所以没有提交记录可用于还原历史。以下时间线只采用本地可核对证据：带日期的验收报告、结果 JSON、回退快照说明和源码修改时间。没有证据的日期明确写为“无可审计完成节点”，不把计划或文件名误报成完成。

## 每日节点

| 日期 | 可审计节点 | 结论 |
| --- | --- | --- |
| 08-16 | 可达俯抓场景、三指手抓取/放置脚本与对应逻辑测试形成；留下多轮 `pose_plan_only`、`contact_only`、`pick_place` JSON。 | 仿真三指手抓放基线形成，但多轮结果并非全部成功，不能概括为“真实抓取完成”。 |
| 08-17 | 无可审计完成节点。 | 不推断。 |
| 08-18 | 无可审计完成节点。 | 不推断。 |
| 08-19 | 无项目完成报告；仅发现一个误下载产生的 0 字节残留，已在本轮清理删除。 | 不计为里程碑。 |
| 08-20 | 新增 HaMeR 手姿消息、手势隔离、共享控制、Servo 输出适配、安全启动脚本和离线演示；阶段一报告记录感知 133 项、仿真 111 项测试通过，离线链达到 50 Hz 控制 tick。 | “最小干预共享遥操作 Stage 1”软件链形成。 |
| 08-21 | 增加手存在门、控制门、活动手选择、SO(3) 因果滤波、腕/前臂融合、现场 Gazebo 验收器和实时 Servo profile；完成 Teleoperation Core V8.3 静态审查与隔离复现。 | D455 + HaMeR/MANO 到安全 Gazebo 跟随的集成入口形成；现场 USB3/MANO 贴合精度仍未正式验收。 |
| 08-22 | 无可审计完成节点。 | 不推断。 |
| 08-23 | 地面抓取场景、工作空间世界和抓取姿态规划器进入工程，建立映射调整前快照。 | 地面工作空间专项开始，尚未以 08-23 单日结果宣称通过。 |
| 08-24 | 地面工作空间验收 JSON 为 `passed=true`：61 项测试、6+6 个离线边界检查、UDP→Servo→Gazebo 链通过；随后完成小幅刚性/位姿解耦、启动零位门、稳定 EGM 位置保持和透视轴解耦。最终轴解耦 JSON 六轴均通过，Servo 危险状态为空，回零误差约 0.204 mm / 0.328°。 | 地面空间映射与稳定位置参考成为可回退、可重复的已验收仿真基线。 |
| 08-25 | 加入地面物体场景、全机器人距离审计、严格自碰撞守卫、预测速度门、手轨迹安全代理和运行时拓扑合约。检查点记录 145 项回归通过、动态预接触停止通过、碰撞手目标在 49/55 采样处被拒绝、空闲 5 秒关节漂移为 0。 | 全机器人自碰撞安全链形成；外部场景碰撞和实体硬件验收不在该检查点范围。 |

## 近 10 天形成的里程碑

1. **M1：三指手仿真抓放基线**（08-16）

   从单纯位置目标推进到可达场景、接触检查和 pick/place 逻辑测试。

2. **M2：共享遥操作 Stage 1**（08-20）

   形成 HaMeR 消息、六维映射、手势隔离、共享控制、Servo 输出门和 50 Hz 离线控制链。

3. **M3：D455/HaMeR 安全 Gazebo 集成**（08-21）

   将手存在、活动手、因果滤波、前臂融合、跨时钟门禁和现场验收串成可运行入口。

4. **M4：地面工作空间 + EGM 稳定位置参考**（08-24）

   通过可重复仿真验收，解决工作空间边界、C-zero 时序、轴耦合和失去输入后的稳定保持。

5. **M5：全机器人自碰撞失败关闭链**（08-25）

   Servo 输出不再直接到控制器；严格守卫、预测采样、手臂互锁和拓扑合约共同把危险命令挡在控制器之前。

6. **M6：可上传的统一版本基线**（08-25，本轮）

   清理绝对路径、Catkin 空模板、嵌套 Git 元数据和误下载残留；隔离大模型/数据/备份/构建产物；补齐手掌碰撞网格的失败关闭分析 proxy，把原先漏跑的仿真测试纳入一键回归，并建立根 Git 仓库。

## 当前未完成，不应包装成里程碑

- D455 USB3 正式认证与长时间现场稳定性复核。
- 真人实时链在当前最终安全配置下的完整人工/机器联合验收。
- 相机范围的逐操作者正式标定（部分轴仍使用 provisional 参数）。
- 真实三指手高层命令适配、应变片标定和抓取成功判据冻结。
- 实体 ABB 授权、逐级低速验收与生产安全评审。

## 关键证据

- `docs/13_SHARED_TELEOP_STAGE1_REPORT.md`
- `docs/15_TELEOPERATION_UBUNTU_CORE_AUDIT.md`
- `docs/17_LIVE_HUMAN_GAZEBO_TELEOP_ACCEPTANCE.md`
- `src/handarm_moveit_demo/README_GROUND_WORKSPACE.md`
- `src/handarm_moveit_demo/docs/18_EGM_POSITION_REFERENCE_PROFILE.md`
- `results/axis_decoupled_mapping/gazebo_direction_validation_20260824.json`
- `results/axis_decoupled_mapping/offline_validation_20260824.json`
- 本机回退说明：`backups/*20260824*.README.txt`、`backups/self_collision_safety_passed_20260825_210302.README.txt`（备份目录不上传）
